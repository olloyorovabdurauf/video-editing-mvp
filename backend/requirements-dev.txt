-r requirements.txt

# Test runner + assertions
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# In-memory Redis. The [lua] extra pulls in lupa so EVALSHA works —
# required because billing.hold + rate_limit use Lua scripts for atomicity.
fakeredis[lua]==2.25.1

# HTTP client used in some test helpers
httpx==0.27.2
