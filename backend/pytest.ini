[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
addopts = -ra --strict-markers
markers =
    slow: marks tests that hit external services (skipped by default)
